# Remote Target Example

Use the Traefik example as a public remote target template. Configure your own
domain, keep secrets in environment variables, and do not embed credentials in
the compose file.
